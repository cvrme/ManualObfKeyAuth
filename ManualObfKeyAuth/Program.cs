using S = System;

public class P
{
    public static void Main()
    {
        O.U1();
        O.U2();
        S.Console.Write(A.C1()); // This should print "Enter Key: "
        string m = Console.ReadLine();
        if (M.L1(m)) // This checks if the input is "123"
        {
            M.P1(); // Junk method
            S.Console.WriteLine(E.C3()); // This should print "good!"
            return;
        }
        F1.R(); // Spam "NO"
    }
}
